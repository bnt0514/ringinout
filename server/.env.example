# === RingInOut Server 환경변수 ===
# ⚠️ 이 파일(.env.example)은 템플릿입니다. .env로 복사 후 실제 값을 입력하세요.
# ⚠️ .env 파일은 절대 Git에 커밋하지 마세요.

# HMAC 비밀키 - anon_user_id 생성에 사용 (32자 이상 랜덤 문자열)
SERVER_SECRET=CHANGE_ME_TO_RANDOM_SECRET_32CHARS_OR_MORE

# Firebase 서비스 계정 키 JSON 파일 경로
FIREBASE_SERVICE_ACCOUNT_PATH=./firebase-service-account.json

# 서버 포트
PORT=3000

# 허용 origin (Flutter 앱에서는 보통 필요 없지만, 웹 테스트용)
ALLOWED_ORIGINS=http://localhost:3000
